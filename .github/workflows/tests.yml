name: tests
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
jobs:
  install-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ["3.12", "3.13"]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: school logger
    steps:
      - uses: actions/checkout@v4
        with:
          path: school logger
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: school logger/requirements.txt
      - name: Install the pinned dependencies and build the package
        run: python -m pip install -r requirements.txt .
      - run: python -m pip check
      - run: python -m pytest -q
      - name: Verify installed assets, UDP, API, export, backup, and restart
        run: python scripts/verify-install.py
      - name: Test the Windows setup entry point
        if: runner.os == 'Windows' && matrix.python == '3.12'
        shell: cmd
        run: call "Setup Windows.cmd" --no-pause
